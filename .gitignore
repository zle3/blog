# Editor / tool settings
.vscode/
.claude/
.idea/
*.code-workspace

# Private source notes, not for publishing
BLOG-NOTES.md
.reference/

# Hugo build output (Cloudflare Pages rebuilds this on every deploy)
zachleinfo/public/
zachleinfo/resources/
zachleinfo/.hugo_build.lock
